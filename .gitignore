*.iml
.gradle
.kotlin
/local.properties
/.idea
/.vscode
/.agents
/.claude
.DS_Store
/build
/app/release/
/captures
.externalNativeBuild
.cxx
local.properties

# IDE inspection report exports
/index.html
/script.js
/styles.css
# The same export also drops one XML per inspection in the project root (plus .xml and
# .descriptions.xml). No tracked XML lives at the root, so ignoring them all is safe.
/*.xml
/.xml
/.descriptions.xml

# MkDocs build output (docs/ itself is tracked; this is what `mkdocs build` renders)
/site
